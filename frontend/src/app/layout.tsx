import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Synq -- AI Implementation Studio",
  description:
    "We build production AI systems inside your business. Multi-agent workflows, real integrations, governed and observable. Live in weeks, not quarters.",
  openGraph: {
    title: "Synq -- AI Implementation Studio",
    description:
      "Production AI systems. Multi-agent workflows. Live in weeks, not quarters.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable}`}
    >
      <body className="min-h-screen bg-background text-foreground antialiased">
        {children}
      </body>
    </html>
  );
}
